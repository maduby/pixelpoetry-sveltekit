node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright CLI logs / screenshots
.playwright-cli/
output/playwright/

# Scratch / tool dirs
.env.local
.claude/
.pnpm-store/
posthog-setup-report.md

# Per-explainer raw research PDFs (large, often copyrighted).
# The .gitkeep stays so the folder structure survives.
docs/explainers/*/sources/*.pdf
docs/sources/*.pdf

# Large source assets (optimised versions committed instead)
static/favicon.zip
static/favicon-16x16.zip
