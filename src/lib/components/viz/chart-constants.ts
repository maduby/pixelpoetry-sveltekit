/** Fixed internal coordinate width — matches ChartContainer's viewBox. */
export const CHART_W = 800;
/** Fixed internal coordinate height — matches ChartContainer's viewBox. */
export const CHART_H = 500;
